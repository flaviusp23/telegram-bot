# Railway Environment Variables Example

# Environment: DEV or PROD
ENVIRONMENT=PROD

# Get token from @BotFather on Telegram
BOT_TOKEN=your-telegram-bot-token

# Railway provides DATABASE_URL automatically
# Format: mysql://user:password@host:port/database
DATABASE_URL=

# Encryption key for sensitive data (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# Google Gemini API Key (get from https://makersuite.google.com/app/apikey)
GOOGLE_API_KEY=

# Admin Panel Configuration (Railway should have this as ADMIN_SECRET_KEY, not JWT_SECRET_KEY)
ADMIN_SECRET_KEY=your-jwt-secret-key

# Admin User Credentials (auto-created on deployment)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=SecurePassword123!

# Optional: Port for admin panel (Railway sets this automatically)
PORT=8000